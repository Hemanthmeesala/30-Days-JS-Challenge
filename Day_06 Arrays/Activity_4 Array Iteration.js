// Task-10 

// use a for loop to iterate over the array and log each element to the console

const arr = [1, 2, 3, 4, 5];

for (let i = 0; i < arr.length; i++) {
  console.log("Array Element : ", arr[i]);
}

// Task-11 

//use the forEach method to iterate over the array and log the each element to the console


arr.forEach((element) => console.log(element));
