// Task-1 

// Create an array of numbers from 1 to 5 and log to the console

const arr = [1, 2, 3, 4, 5];
const data = () => {
  return arr;
};
console.log(data());

// Task-2 

// access the first and last element of the array and log them to the console


console.log(`First element of array is ${arr[0]}`);
console.log(`Last element of array is ${arr[arr.length - 1]}`);
